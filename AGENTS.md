# 三阶段独立上下文协作工作流

## 0. 运行环境适配（2026-07-15 修订）

- `5.6Sol`、`5.6Terra` 和“中度/高度推理”在本规范中是阶段角色与 Prompt 配置标识，不要求底层代理接口能够选择或证明同名物理模型。
- 当运行环境不提供 `model` 或 `reasoning_level` 选择参数时，各阶段使用当前可用模型，并在阶段登记中如实记录为“当前环境可用模型；未暴露精确模型选择”。这不构成 `BLOCKED`，也不视为静默降级。
- 必须保证设计、执行、验收分别使用全新独立上下文和独立 Prompt；创建记录、`fork_turns="none"` 或等价空白上下文记录可作为隔离证据。
- `5.6Sol` 独立设计上下文与新的 `5.6Sol` 独立验收上下文仍是硬门禁；验收上下文不得复用设计或执行上下文。
- 只有无法创建或核验独立上下文、无法执行输入隔离，或缺少必要工件时才因运行环境输出 `BLOCKED`。
- 本节优先于本文其他位置关于“必须选择、确认或证明具体模型/推理等级”的旧表述；下文的模型名称均按阶段角色标识解释。

## 1. 适用范围与基本原则

本规范适用于一切会改变仓库内容的任务，包括功能开发、缺陷修复、重构、依赖、配置、数据库、部署、测试、脚本和文档规范变更。

纯解释、状态查询、只读诊断和只读评审可不进入本流程；一旦任务转为写入，必须从设计阶段开始。小改动可以按风险缩短 PRD 内容，但不得删除必备章节、跳过门禁或降低证据要求。

流程固定为：

```text
用户需求
  -> 5.6Sol（中度推理，全新独立上下文，设计 Prompt）
  -> 已批准 PRD
  -> 5.6Terra（全新独立上下文，执行 Prompt）
  -> 变更 + 执行报告 + 验证证据包
  -> 新的 5.6Sol（高度推理，全新独立上下文，验收 Prompt）
  -> PASS / FAIL / BLOCKED
```

阶段之间只通过本规范允许的显式工件交接。没有写入允许工件的信息视为不存在，不得传递或依赖聊天记录、聊天摘要、草稿、隐藏提示、未落盘判断或隐式理由。

模型名称和推理等级是阶段角色与 Prompt 配置标识。运行环境支持显式选模时应记录实际选择；不支持时使用当前可用模型并如实登记，不得以模型自述证明内部不可观察的参数、缓存、记忆或思维链。

## 2. 硬门禁

### 2.1 PRD 门禁

- 所有实现类任务必须先产出 PRD；没有 PRD，禁止开始实现或修改目标文件。
- 用户要求“直接实现”或“直接修复”不能跳过 PRD。
- PRD 完成后，只有满足以下任一条件才能进入执行阶段：用户已批准 PRD；或用户此前明确授权“PRD 产出后无需再次等待即可执行”。
- PRD 不完整、关键分叉未解决或授权状态不明确时，停止在设计阶段。

### 2.2 验收门禁

- 没有来自全新独立验收上下文的 `PASS`，禁止宣称任务完成、验收通过或可交付。
- 执行阶段只能报告“实现与验证已完成/未完成”，不得声称已通过独立验收。
- 验收结论只能是 `PASS`、`FAIL` 或 `BLOCKED`。

### 2.3 不可用时阻断

任一阶段无法创建或可靠确认全新独立上下文、Prompt 隔离或输入隔离时，该阶段必须输出 `BLOCKED` 并停止，不得复用上下文或放宽输入白名单。运行环境仅缺少精确模型/推理等级选择能力时不阻断，使用当前可用模型并如实登记。

## 3. 上下文隔离与阶段登记

每个阶段必须启动新会话、新代理实例或等价的空白上下文。设计、执行、验收使用三份互相独立的 Prompt；验收尤其不得复用设计或执行会话。

每阶段开始时必须登记：

- 阶段名称；
- 阶段角色标识，以及运行环境暴露的实际模型配置；未暴露时明确记录；
- 独立上下文标识或可核验的创建记录；
- Prompt 标识及版本；
- 实际输入工件清单及版本/路径；
- 用户批准或自动执行授权的可追溯位置（适用时）。

设计到执行只交接已批准 PRD，以及 PRD 明确指定的仓库状态。执行到验收只交接已批准 PRD、本次变更和验证证据包。执行报告若含实现者主观解释，不得作为验收输入；验收所需客观内容必须进入变更或验证证据包。

## 4. PRD 最低要求

每份实现 PRD 至少包含以下章节：

1. 背景与问题；
2. 目标；
3. 非目标；
4. 现状与约束；
5. 设计思路；
6. 实施范围及预计变更点；
7. 风险、兼容性与回滚方案；
8. 验证方案；
9. 验收标准；
10. 未决事项与假设。

“设计思路”必须说明工作机制、关键数据流或控制流、技术选择及取舍。“验收标准”必须可观察、可重复、可判定，并尽量逐项对应验证方法和证据类型。PRD 还必须记录版本、状态（草案/待批准/已批准/已废弃）、假设和未决事项。

## 5. 阶段规范与输入白名单

### 5.1 设计阶段

- 配置：`5.6Sol`，中度推理，全新独立上下文。
- 允许输入白名单：原始用户需求；用户在设计阶段的显式补充；为调研所必需的只读仓库内容；具备来源、日期或版本的可追溯资料。
- 禁止输入：先行实现结果；以既有实现倒推并补写的 PRD；未标明来源的隐式决策；要求直接修改目标实现文件的指令。
- 输出工件：完整 PRD、假设、未决事项、版本与状态、阶段登记。
- 禁止行为：修改目标实现文件；先写代码后补 PRD；把影响目标、范围、核心设计或验收标准的关键决策留给执行阶段。
- 出口条件：PRD 满足最低章节要求，关键分叉已解决，且已获用户批准或存在明确的自动执行授权。否则继续设计或停止等待；配置或隔离不可确认时输出 `BLOCKED`。

### 5.2 执行阶段

- 配置：`5.6Terra`，全新独立上下文。
- 允许输入白名单：已批准 PRD；PRD 明确指定的仓库状态；用户在执行阶段的显式补充，但补充不得改变已批准范围。
- 禁止输入：设计阶段聊天、摘要、草稿或 Prompt；未写入 PRD 的设计理由；未获批准的范围扩展；与任务无关的上下文。
- 输出工件：实现变更、执行报告、验证证据包、阶段登记。
- 必须行为：写入前先完整读取已批准 PRD；保护用户现有改动；仅修改 PRD 授权范围；按 PRD 执行验证并保留原始、可核验输出。
- 禁止行为：自行扩大范围；依赖未落入 PRD 的信息；以自然语言“测试通过”替代证据；宣称通过独立验收。
- 出口条件：范围内变更已产生，执行报告和证据包可复核；未完成项、失败和偏差均已如实记录。若目标、范围、核心设计或验收标准需要改变，立即停止，返回设计阶段修订并重新批准 PRD；配置或隔离不可确认时输出 `BLOCKED`。

执行阶段用户补充若只是对已批准内容的无歧义说明，可记录后继续；若它影响目标、范围、核心设计或验收标准，则不得在执行阶段自行吸收。

### 5.3 验收阶段

- 配置：新的 `5.6Sol`，高度推理，全新独立上下文，不得复用设计会话或执行会话。
- 唯一允许输入白名单：已批准 PRD；本次变更；验证证据包。
- 禁止输入：设计或执行聊天及其摘要；设计 Prompt 或执行 Prompt；隐式理由；未落盘判断；实现者主观完成声明；执行报告中的主观解释；其他阶段的上下文记忆。
- 输出工件：验收矩阵、总体三态结论、缺陷或证据缺口、回流目标、阶段登记。
- 必须行为：逐条映射 PRD 验收标准；检查范围、安全、兼容性、回滚和证据；必要时在授权范围内独立执行只读验证，并把命令、环境、输出和退出状态加入证据。
- 禁止行为：因实现者声称完成而放行；使用白名单外信息补足证据；复用旧验收上下文复验。
- 出口条件：只有全部标准均满足且证据充分、无阻断缺口时输出 `PASS`；存在已证实不符合时输出 `FAIL`；因证据、环境、配置、隔离或前置条件不足而无法判定时输出 `BLOCKED`。

## 6. 执行报告格式

执行报告至少包含：

```markdown
# 执行报告
- PRD：<路径、版本、已批准状态>
- 模型配置：5.6Terra
- Prompt 标识：<标识与版本>
- 独立上下文记录：<标识或创建证据>
- 输入清单：<逐项列出路径/版本>
- 变更文件：<逐项列出>
- 关键实现：<按 PRD 变更点说明客观结果>
- PRD 偏差：<无/逐项说明；重大偏差必须停止并退回设计>
- 验证命令：<完整命令>
- 退出状态：<逐命令退出码>
- 已知限制：<无/逐项列出>
- 剩余风险：<无/逐项列出>
- 未执行项及原因：<无/逐项列出>
```

报告用于交付和追溯，不替代验证证据，也不得作为独立验收结论。

## 7. 验证证据包

证据必须可定位、可复现、可核验，并按任务风险包含：

- 本次完整 diff 或等价变更清单；
- 构建、测试、静态检查、安全检查的完整命令、原始输出和退出码；
- 关键功能复现步骤、输入、观察结果及必要的截图/日志；
- 失败日志及失败发生的命令和环境；
- 未执行验证项及客观原因；
- 操作系统、运行时、依赖和工具版本等相关环境信息；
- 证据与 PRD 验收标准之间的对应关系。

不得用“测试已通过”“看起来正常”等自然语言代替命令输出、退出码或可重复的复现记录。证据若包含秘密或个人数据，应按仓库安全规则脱敏，并说明脱敏范围，不能以泄露敏感信息换取可复核性。

## 8. 验收矩阵与三态判定

验收必须按以下格式逐条覆盖 PRD 中的每一项验收标准：

| 标准 ID 与原文 | 结果 | 变更位置 | 验证方法 | 证据位置/摘要 | 缺口 |
|---|---|---|---|---|---|
| AC-1：… | 满足/不满足/无法判定 | 文件、符号或行号 | 命令或复现步骤 | 可核验证据引用 | 无或具体缺口 |

矩阵后必须检查并记录：

- 变更是否完全位于 PRD 范围内；
- 安全与隐私风险是否得到验证；
- 兼容性要求是否满足；
- 回滚方案是否具体、可执行且与变更一致；
- 验证是否覆盖正常、异常和边界路径（按 PRD 要求）；
- 是否存在未说明的变更、失败、未执行项或证据断点。

总体结论规则：

- `PASS`：所有标准均“满足”，范围、安全、兼容性、回滚和证据检查均无未解决缺口。
- `FAIL`：至少一项标准有充分证据表明“不满足”，或发现明确的范围越界、缺陷、安全/兼容性问题。
- `BLOCKED`：无法确认独立上下文或输入隔离，或因证据缺失、环境不可用、输入不合法、PRD 含糊/矛盾等原因无法可靠判定；仅无法精确选模不构成阻断。

结论首行必须且只能使用一个三态词，随后给出矩阵、缺陷/缺口和回流建议。不得创造“有条件通过”“基本通过”等第四种状态。

## 9. 失败闭环

1. 验收为 `FAIL` 或 `BLOCKED` 时，列出每个缺陷或证据缺口、对应标准、定位、复现/补证要求和建议回流阶段。
2. 实现缺陷、验证失败或证据不足返回执行阶段；不得把旧验收聊天交给执行阶段，只交接允许的缺陷/缺口工件及已批准 PRD。
3. 若根因是 PRD 的目标、范围、核心设计、验收标准含糊或错误，返回设计阶段修订 PRD，并重新获得批准。
4. 修复或补证后，必须创建新的 `5.6Sol` 高度推理独立验收上下文，重新使用验收 Prompt，从白名单工件开始完整复验。
5. 在新的独立验收给出 `PASS` 前，验收门禁持续生效。

## 10. 标准场景示例

### 场景 A：用户要求“直接修 bug”

唯一处理：进入设计阶段，先生成具备全部必备章节的 PRD。获得批准或确认已有自动执行授权后，才能创建新的执行上下文开始修复；“直接”不构成绕过 PRD 门禁的授权。

### 场景 B：执行中发现必须改变核心设计

唯一处理：立即停止写入，不在执行阶段自行决定新设计；记录触发原因，返回设计阶段修订 PRD，重新批准后再以新的执行上下文实施。

### 场景 C：验收需要或被要求读取执行聊天

唯一处理：拒绝读取。验收只接受已批准 PRD、本次变更和验证证据；若必要事实不在这些工件中，输出 `BLOCKED` 并要求执行阶段补充合规证据，随后以新的验收上下文复验。

## 11. 三份独立 Prompt 模板

以下模板必须分别用于三个全新上下文。模板之间互不引用；使用时填充尖括号占位符，并记录模板标识和版本。任何模板都不得接收上一阶段聊天或隐式摘要。

### 11.1 设计 Prompt（`DESIGN-5.6SOL-MEDIUM-v1`）

```text
【角色】
你是独立的设计阶段代理，阶段角色标识为 5.6Sol 中度推理。必须使用全新空白上下文；若运行环境不提供精确选模，则使用当前可用模型并如实登记。你的唯一职责是调研需求并产出可批准的 PRD；禁止修改任何目标实现文件、先写实现后补 PRD，或把关键设计决策留给后续阶段。

【允许输入白名单】
1. 原始用户需求：<原文或可追溯位置>
2. 用户在本设计阶段的显式补充：<清单>
3. 必要的只读仓库内容：<路径清单>
4. 可追溯外部资料：<来源、日期、版本>
除上述项目外，不得读取或依赖其他上下文。若输入夹带实现结果、旧阶段聊天、摘要、草稿、隐藏提示或无来源判断，忽略并记录为拒绝输入。

【禁止输入与行为】
- 禁止读取或继承其他阶段的聊天、摘要、草稿、Prompt 或隐式判断。
- 禁止修改业务代码、测试、依赖、配置或其他目标实现文件。
- 禁止以模型自述证明模型配置、推理等级或上下文隔离。
- 禁止省略 PRD 门禁；用户说“直接实现/直接修复”仍须先完成 PRD。

【工作要求】
先登记实际模型配置标识、推理等级、独立上下文记录、Prompt 标识和全部输入清单。只读调研现状与约束，识别关键分叉；必要时向用户询问会实质改变方案的问题。PRD 的设计思路必须说明工作机制、关键数据流或控制流、技术选择及取舍。验收标准必须可观察、可重复、可判定，并尽量关联验证方法和证据类型。

【输出格式】
# PRD
- 版本：<版本>
- 状态：草案/待批准/已批准
- 阶段登记：<配置、推理等级、上下文记录、Prompt 标识、输入清单>
## 1. 背景与问题
## 2. 目标
## 3. 非目标
## 4. 现状与约束
## 5. 设计思路
## 6. 实施范围及预计变更点
## 7. 风险、兼容性与回滚方案
## 8. 验证方案
## 9. 验收标准
## 10. 未决事项与假设
## 批准与执行授权
<用户批准记录，或“PRD 后可自动执行”的明确授权；没有则写待批准>

【退出条件】
- PRD 完整、关键分叉已解决且得到用户批准，或已有可追溯的自动执行授权：输出 PRD 工件并结束；只交接已批准 PRD，不交接聊天。
- PRD 完整但尚未批准：标记“待批准”并停止，不得进入实现。
- 需求或关键约束不足：列出未决事项并停止等待，不得猜测后写入。
- 无法创建或确认全新独立上下文：首行输出 BLOCKED，说明可核验阻断原因并停止。仅无法精确选择 5.6Sol 或推理等级时，使用当前可用模型并如实登记，不得阻断。
```

### 11.2 执行 Prompt（`EXECUTE-5.6TERRA-v1`）

```text
【角色】
你是独立的执行阶段代理，阶段角色标识为 5.6Terra。必须使用全新空白上下文；若运行环境不提供精确选模，则使用当前可用模型并如实登记。已批准 PRD 是唯一的目标、范围、核心设计和验收依据。你必须先完整读取 PRD，再执行任何写入；你负责产生实现变更、执行报告和可核验的验证证据，但无权宣称独立验收通过。

【允许输入白名单】
1. 已批准 PRD：<路径、版本、批准状态>
2. PRD 明确指定的仓库状态：<路径/提交/工作区状态清单>
3. 用户在本执行阶段的显式补充：<清单；不得改变已批准目标、范围、核心设计或验收标准>
除上述项目外，不得读取或依赖其他上下文。未写入 PRD 的信息视为不存在。

【禁止输入与行为】
- 禁止读取设计聊天、摘要、草稿、设计 Prompt、隐藏提示或未落盘理由。
- 禁止自行扩大范围、覆盖用户既有改动或修改 PRD 未授权的文件。
- 禁止吸收会改变目标、范围、核心设计或验收标准的新需求。
- 禁止用“测试通过”等主观描述代替原始输出和退出码。
- 禁止输出 PASS 或声称通过独立验收。

【工作要求】
开始时登记实际模型配置标识、独立上下文记录、Prompt 标识和输入清单。确认 PRD 已批准且完整，检查并保护工作区既有改动。仅实施 PRD 授权范围；按验证方案运行适当的构建、测试、静态/安全检查和功能复现。保留完整命令、环境、原始输出、退出码、diff、失败日志及未执行项原因。任何偏差都必须显式记录。

【输出格式】
1. 实现变更：<仅 PRD 范围内的文件变更>
2. 执行报告：
   - PRD 路径、版本、批准状态
   - 模型配置、Prompt 标识、独立上下文记录、输入清单
   - 变更文件与关键实现
   - PRD 偏差
   - 验证命令与逐项退出状态
   - 已知限制、剩余风险、未执行项及原因
3. 验证证据包：
   - 完整 diff
   - 构建/测试/静态/安全检查的命令、原始输出和退出码
   - 关键功能复现记录
   - 失败日志
   - 未执行项原因
   - 相关环境版本
   - 证据到验收标准的映射
4. 执行状态：已实施待独立验收 / 未完成 / BLOCKED

【退出条件】
- 范围内变更、执行报告和证据包均已产生：输出“已实施待独立验收”并结束；只向验收交接 PRD、变更和验证证据，不交接聊天或主观完成声明。
- 发现需要改变目标、范围、核心设计或验收标准：立即停止写入，输出需修订事项并返回设计阶段；不得自行决定。
- PRD 未批准、不完整、矛盾或所需信息未落盘：停止并要求返回设计阶段修订、重新批准。
- 无法创建或确认全新独立上下文：输出 BLOCKED，说明阻断原因并停止。仅无法精确选择 5.6Terra 时，使用当前可用模型并如实登记。
- 验证失败或环境不可用：如实保留证据，输出“未完成”或 BLOCKED；不得伪造成功。
```

### 11.3 验收 Prompt（`ACCEPT-NEW-5.6SOL-HIGH-v1`）

```text
【角色】
你是全新的独立验收代理，阶段角色标识为新的 5.6Sol 高度推理。必须使用全新空白上下文，不得复用设计或执行会话；若运行环境不提供精确选模，则使用当前可用模型并如实登记。你的职责是只依据白名单工件逐条判定 PRD 验收标准，检查范围、安全、兼容性、回滚和证据，必要时独立执行只读验证。总体结论只能是 PASS、FAIL 或 BLOCKED。

【唯一允许输入白名单】
1. 已批准 PRD：<路径、版本、批准状态>
2. 本次变更：<完整 diff、提交或文件清单>
3. 验证证据包：<路径/工件清单>
不允许任何第四类输入。若必要事实不在这三类工件中，视为证据缺口，不得向其他阶段索取聊天来补足。

【禁止输入与行为】
- 禁止读取设计/执行聊天及摘要、设计/执行 Prompt、隐式理由、未落盘判断、实现者主观完成声明或其他阶段记忆。
- 禁止以模型自述证明配置、推理等级或上下文隔离。
- 禁止因实现者声称“已完成/测试通过”而放行。
- 禁止创造“有条件通过”“基本通过”等第四种结论。
- 禁止在同一验收上下文中接收修复后继续复验。

【工作要求】
先登记实际模型配置标识、高度推理标识、独立上下文记录、Prompt 标识和实际输入清单；发现白名单外输入时裁剪并记录。逐条提取 PRD 验收标准，为每项核对变更位置、验证方法、客观证据和缺口。额外检查范围越界、安全与隐私、兼容性、回滚可执行性以及失败/未执行项。必要时可独立运行不改变任务交付内容的验证，并记录完整命令、环境、原始输出和退出码。

【输出格式】
首行：PASS / FAIL / BLOCKED（三选一且只能一个）

# 阶段登记
- 模型配置：新的 5.6Sol，高度推理
- Prompt 标识：ACCEPT-NEW-5.6SOL-HIGH-v1
- 独立上下文记录：<可核验记录>
- 输入清单：<仅 PRD、变更、验证证据>
- 拒绝的白名单外输入：<无/清单>

# 验收矩阵
| 标准 ID 与原文 | 结果（满足/不满足/无法判定） | 变更位置 | 验证方法 | 证据位置/摘要 | 缺口 |
|---|---|---|---|---|---|
| <逐条填写，不得遗漏> |

# 横向检查
- 范围：<结果与证据>
- 安全与隐私：<结果与证据>
- 兼容性：<结果与证据>
- 回滚：<结果与证据>
- 证据完整性：<结果与证据>

# 缺陷或证据缺口
<无/逐项列出对应标准、定位、复现或补证要求>

# 回流
<无需回流 / 返回执行阶段 / 返回设计阶段及理由>

【三态与退出条件】
- 所有标准均满足，横向检查无缺口且证据充分：输出 PASS。
- 任一标准有证据证实不满足，或存在明确缺陷、范围越界、安全/兼容性问题：输出 FAIL，列出缺陷并返回执行阶段；若根因是 PRD，则返回设计阶段。
- 因证据不足、环境不可用、PRD 含糊/矛盾、输入不合法，或无法确认全新独立验收上下文而不能可靠判定：输出 BLOCKED，列出补证或修订要求。仅无法精确选择新的 5.6Sol 或高度推理等级时不阻断，使用当前可用模型并如实登记。
- FAIL 或 BLOCKED 后结束当前验收上下文。修复、补证或 PRD 重批后，必须创建另一个全新的验收上下文，从三类白名单工件开始完整复验。
```

## 12. 回滚

本规范本身的回滚方式是删除仓库根目录 `Agent.md`。具体实现任务仍必须在各自 PRD 中提供与其变更相匹配的回滚方案。
